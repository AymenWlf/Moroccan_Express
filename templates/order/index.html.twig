{% extends 'base.html.twig' %} {% block title %}Valider ma commande - MoroccanExpress{% endblock %} {% block content %}
<h1>Je passe ma commande</h1>
<p>Choisisesez vos préférences avant de passer votre commande sur MoroccanExpress</p>
<hr>
<div class="row">
    <div class="col-md-6">
        <div class="card-address-sty">
            {% set formHTML %} {{form_start(form , {action:path('order_recap')})}} {{form_label(form.addresses , 'Choisissez votre adresse de livraison')}}
            <a href="{{path('account_address_add')}}">Ajouter une nouvelle adresse</a> {{form_end(form)}} {% endset %} {{formHTML|replace({'[br]' : '<br/>'})|raw}}
        </div>
    </div>
    <div class="col-md-6">
        <div class="text-center">
            <b>Récap de ma commande</b>
            <p>Voici le récapitulatif de vos produits</p>
        </div>


        <div class="order-sumary">{% for key,product in cart %}
            <div class="row {% if key > 0 %}mt-2 {% endif %}">
                <div class="col-md-3">
                    <img src="/uploads/{{product.product.illustration}}" alt="{{product.product.name}}" height="75px" class="mr-2">
                </div>
                <div class="col-md-7 my-auto">
                    {{product.product.name}}<br>
                    <small>{{product.product.name}} </small>
                </div>
                <div class="col-md-2 my-auto">
                    x {{product.quantity}}
                </div>

            </div>{% endfor %}
        </div>

    </div>

    <div class="col-md-6">

    </div>
</div>

{% endblock content %}